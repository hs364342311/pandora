function initConfig(apiService='http://xxx'){
    window.systemConfig = {
        apiService: apiService,
        casService: 'https://xxx',
        home: '/chat',
        footer: 'xxx'
    }
}
initConfig()
